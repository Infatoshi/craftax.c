# craftax.c (retired)

Moved into **https://github.com/Infatoshi/craftax.cu** as a single-file CPU
backend: `craftax.c` (`make`, then `./craftax bench --backend cpu`). Full multi-file history is in
this repo's git log.
